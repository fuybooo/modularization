define(function(require) {
    var app = require('app');
    var $ = require('jquery');
    app.controller('FmServiceController', function ($scope) {
        console.log('FmServiceController');
    });
});