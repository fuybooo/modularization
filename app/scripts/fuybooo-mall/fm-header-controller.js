define(function(require) {
    var app = require('app');
    var $ = require('jquery');
    app.controller('FmHeaderController', function ($scope) {
        console.log('FmHeaderController');
    });
});