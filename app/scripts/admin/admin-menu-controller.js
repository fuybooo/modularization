define(function(require) {
    var app = require('app');
    var $ = require('jquery');
    app.controller('AdminMenuController', function ($rootScope, $scope, $location,$anchorScroll) {
        console.log('AdminMenuController');
        // $('.admin-menu .home-li').click();
        
    });
});